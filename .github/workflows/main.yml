name: Workflow with Email Identifier

on:
  workflow_dispatch:  # manual trigger
  push:
    branches: [ main ]  # runs on push to main

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: 23f2001456@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Display date
        run: date

